<%= form_with(model: registro_medicion, local: true) do |form| %>
  <% if registro_medicion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registro_medicion.errors.count, "error") %> prohibited this registro_medicion from being saved:</h2>

      <ul>
      <% registro_medicion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :valor %>
    <%= form.text_field :valor, id: :registro_medicion_valor %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo, id: :registro_medicion_tipo %>
  </div>

  <div class="field">
    <%= form.label :unidad %>
    <%= form.text_field :unidad, id: :registro_medicion_unidad %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
